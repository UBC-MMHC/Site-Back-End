# Local dev overrides - create local.properties or use env vars
spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/membership_db
    username: postgres
    password: postgres
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
