# Local dev overrides - use with docker postgres (site_db)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/site_db
    username: postgres
    password: postgres
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
